{
  "name": "@resolvai/shared-schemas",
  "version": "0.1.0",
  "description": "Shared schemas, prompt configurations, and type definitions for ResolvAI projects",
  "main": "dist/index.ts",
  "types": "dist/index.ts",
  "exports": {
    ".": {
      "types": "./dist/index.ts",
      "default": "./dist/index.ts"
    },
    "./db-schema": {
      "types": "./dist/db-schema/index.ts",
      "default": "./dist/db-schema/index.ts"
    },
    "./config": {
      "types": "./dist/config/index.ts",
      "default": "./dist/config/index.ts"
    },
    "./shared-types": {
      "types": "./dist/shared-types/index.ts",
      "default": "./dist/shared-types/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "schemas",
    "prompts",
    "database",
    "types",
    "shared",
    "typescript",
    "drizzle"
  ],
  "license": "MIT",
  "peerDependencies": {
    "drizzle-orm": "^0.44.7"
  },
  "devDependencies": {
    "@types/node": "^22.19.1",
    "drizzle-kit": "^0.31.7",
    "typescript": "^5.0.0"
  }
}
